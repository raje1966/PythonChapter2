is - if two variables reference to same object, it will return as true, otherwise it will return false. e.g. a=5, b=6, a is b will return as false.

is not - If two variables not referencing to same object, it will return true, otherwise false. e.g. a = 5, b = 6, so a is not b will return true.